#include<stdio.h>

typedef struct node
{
    int size;
    int top = -1;
}NODE;


void push(NODE *arr, int number){
    ++arr[i].top

}
void pop{

}

int main()
{
    int n, number;
    printf("Enter the number of elements in the stack:\n");
    scanf("%d",&n);
    NODE *arr = malloc(n*sizeof(NODE));
    printf("Enter the number you want to print in the stack:\n");
    scanf("%d",&number);
    push(arr, number);
    display(arr);

}